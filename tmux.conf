bind r source ~/.config/tmux/tmux.conf

# run 'ls'
set -g mode-keys vi
set -g default-terminal "${TERM}"
set -sg terminal-overrides ",*:RGB"
set -g base-index 1
set -g pane-base-index 1
set -g renumber-windows on

set -g prefix '`'
bind-key '`' send-prefix

run '~/.config/tmux/themes/selector.sh'
